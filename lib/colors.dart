// This are colors that are used in the main app.

import 'package:flutter/material.dart';

// Base color which is used for the card background.
const baseCardColor = Color(0xFFd18585);
// Green color is used for the second element inside the card widget.
const greenCardColor = Color(0xFFa8d8ad);
// Grey color is used for first element inside the card widget.
const greyCardColor = Color(0xFFc4c4c4);